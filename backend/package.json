{
  "name": "photobooth-backend-api",
  "version": "1.0.0",
  "description": "Backend API for an Photo Booth",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "clean": "node scripts/clean.js",
    "clean:safe": "node scripts/clean-interactive.js",
    "clean:dev": "npm run clean && npm run dev",
    "reset": "npm run clean:safe",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "i": "^0.3.7",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nedb": "^1.8.0",
    "npm": "^11.4.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
